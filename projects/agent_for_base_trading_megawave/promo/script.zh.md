# Megawave Web 宣传片中文配音稿

这是 Megawave，一个面向 Base 链交易的 Web 控制台。

市价单可以从表单直接生成标准命令，统一进入报价、风控和订单状态流程。

每一笔交易都会先进入待确认状态，执行前保留人工决策，不绕过安全边界。

限价单由本地 watcher 监控价格，到价后再生成市价单执行。

自然语言不会直接交易，它会先被解析成可审查的标准命令预览。

确认、拒绝、取消这类高风险动作必须手动完成。

订单状态、历史记录和链上链接会被统一沉淀，便于追踪和复盘。

跟单管理也接入同一控制台，可独立配置、暂停和追踪。

